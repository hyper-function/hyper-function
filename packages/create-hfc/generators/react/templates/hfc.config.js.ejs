const shared = ["react", "react-dom", "react-to-hfc"];
let sharedAlias = {};<% if (usePreact) { %>

const runtime = process.env.REACT_RUNTIME || "preact";
if (runtime === "preact") {
  shared[2] = "preact";
  sharedAlias = {
    react: "preact/compat",
    "react-dom/test-utils": "preact/test-utils",
    "react-dom": "preact/compat",
    "react/jsx-runtime": "preact/jsx-runtime",
  };
}<% } %>

module.exports = {
  entry: "./src/index.jsx",
  plugins: [],
  shared,
  sharedAlias,
};
